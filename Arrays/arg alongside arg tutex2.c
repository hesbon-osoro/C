#include<stdio.h>
double average(int arr[],int size);
int main()
{
int balance[5]={1000,2,3,7,50};
double average;
average=average(balance,5);
printf("Average=%f",average);
return o;
}
double average( int balance, int size)
{
int i,size=5;
double sum, average;
for(i=0;i<size;i++)
{
sum+=arr[i];
}
average=sum/size;
return average;
}
